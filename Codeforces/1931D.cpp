#include <algorithm>
#include <math.h>
#include <cstdio>
#include <deque>
#include <iomanip>
#include <iostream>
#include <queue>
#include <map>
#include <list>
#include <set>
#include <sstream>
#include <stack>
#include <string>
#include <vector>
#include <cstring>
#include <climits>
#define ll long long
#define ull unsigned long long
#define FOR(m,s,n,u) for(int m=s; m<n; m+=u)
#define ROF(m,s,n,u) for(int m=n; m>=s; m-=u)
#define FORb(m,s,n,u,k) for(int m=s; m<n&&k; m+=u)
#define pb push_back
#define mp make_pair
#define mod 1000000007
#define PI 3.1415926535897932384626433832795028841971
#define INF 0x3f3f3f3f
#define EPS 1e-9
#define MOD(a,x) ((a%x)+x)%x
#define sync ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL)
#define vi vector<int>
#define pii pair<int,int>
using namespace std;

int main(){
sync;
/*
int t; cin>>t;
while(t--){
    int n, x, y; cin>>n>>x>>y;
    int li[n];

    for(int i = 0; i < n; i++){
        cin>>li[i];
    }
    int acc = 0;
    
    for(int i = 0; i < n; i++){
        for(int j = i + 1; j < n; j++){
            int a, b; a = li[i]; b = li[j];
            if((a + b) % x == 0 && (a - b) % y == 0){
                //cout<<"a: "<<a<<" b: "<<b<<endl;

                acc++;
            }
        }
    }

    cout<<acc<<endl;
}
    */

   cout<<"1 > "<< 1 % 5 << " % 5"<<endl;
   cout<<"1 > "<< 1 % 2 << " % 2"<<endl;

   cout<<"9 > "<< 9 % 5 << " % 5"<<endl;
   cout<<"-8 > "<< -8 % 2 << " % 2"<<endl;

  
return 0;
}