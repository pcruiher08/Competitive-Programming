class program {

define gira(){
if(nextToABeeper()){
pickbeeper();
gira();
turnleft();
}
}

    program () {
    while(nextToABeeper()){
        gira();
        move();
        }
        turnoff();
    }
}