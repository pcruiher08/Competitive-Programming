class program {
    program () {
       while(frontIsClear()){
       if(nextToABeeper()){
       while(nextToABeeper()){
       pickbeeper();
       }
       }
       move();
       }
       while(anyBeepersInBeeperBag()){
       putbeeper();
       }
        turnoff();
    }
}